import 'package:flutter/material.dart';

class SubMenuDetailPage extends StatelessWidget {
  const SubMenuDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
